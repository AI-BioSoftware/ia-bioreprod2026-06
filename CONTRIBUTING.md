# Contributing

## Workflow recommandé

1. Créer une branche dédiée.
2. Modifier le code dans `R/`.
3. Adapter les tests dans `tests/testthat/`.
4. Lancer `devtools::document()`, `devtools::check()`, `lintr::lint_package()`.
5. Ouvrir une pull request.
